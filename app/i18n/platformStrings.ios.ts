/**
 * Platform-specific i18n overrides (iOS)
 *
 * Empty — Apple App Store review rejects bundles containing
 * anonymous login strings. Metro resolves this file for iOS,
 * keeping those strings out of the build entirely.
 */
export const platformStrings: Record<string, Record<string, Record<string, string>>> = {}
